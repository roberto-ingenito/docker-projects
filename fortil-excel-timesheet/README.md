# 📊 Fortil Excel Timesheet - Utility Web

Questa è un'applicazione puramente front-end costruita con React (Vite) per la generazione di un foglio excel, riguardo l'orario lavorativo svolto durante il mese, per fortil.

## ⚙️ Stack Tecnologico

* **Framework:** React con Vite
* **Porta Interna:** `3000` (come da `docker-compose.yml`)

### 🚀 Avvio del Servizio

Per avviare solo questa utility:

```bash
docker compose up -d fortil-excel-timesheet