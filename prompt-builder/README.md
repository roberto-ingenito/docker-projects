# ✍️ Prompt Builder - Strumento di Generazione Prompt

Questo servizio è un'applicazione Next.js per la creazione e la gestione di prompt semplici. Funziona come un'applicazione a pagina singola con logica di rendering lato server (SSR/SSG), completamente containerizzata.

## ⚙️ Stack Tecnologico

* **Framework:** Next.js (React/TypeScript)
* **Contenitore:** Node.js, esposto internamente sulla porta `3000`.

## 🚀 Avvio del Servizio

Per avviare solo il Prompt Builder:

```bash
docker compose up -d prompt-builder